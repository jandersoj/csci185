function toggleFall() {
    /*
        Your task: target the body element and change its class to "fall"
    */
   document.querySelector("body").className = 'fall';
}

function toggleWinter() {
    /*
        Your task: target the body element and change its class to "winter"
    */
        document.querySelector("body").className = 'winter';
}

function toggleSpring() {
    /*
        Your task: target the body element and change its class to "spring"
    */
        document.querySelector("body").className = 'spring';
}

function toggleUnca() {
    /*
        Your task: target the body element and change its class to "spring"
    */
        document.querySelector("body").className = 'unca';
}